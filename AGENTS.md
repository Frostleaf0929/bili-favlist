# 项目规则（AGENTS.md）

本项目由零基础开发者使用 AI 辅助开发。请严格遵守以下规则，任何一条都不可为了"完成任务"而跳过。

## 一、安全底线（不可协商）

1. **密钥与凭据**：所有 API Key、密码、Token 只从环境变量（`.env` 文件）读取。禁止把任何真实密钥写进源码、示例代码、测试代码、README 或对话输出中。需要示例时用 `your-api-key-here` 这样的占位符。
2. **装包前必问**：安装任何第三方包（npm / pip 等）之前，必须先列出：包名、用途、版本、大约的下载量，等我确认后才执行。**禁止全局安装**（不加 `-g` / 不用 `pip install` 裸装到系统）。
3. **删除操作**：删除或覆盖任何已存在的文件前，必须先说明要删什么、为什么，等我确认。
4. **不碰规则外的东西**：只修改任务明确要求的文件。发现"顺便"改了别的文件，要主动指出来。禁止修改 `.git`、`node_modules`、`.env` 以外我不了解的系统级配置。

## 二、动手前先说

5. **改动前报告**：开始写代码前，用一两句话说明：要改哪些文件、实现什么效果。大改动（涉及 3 个以上文件）先列计划再动手。
6. **权限三问自查**：写的代码涉及读取数据时，自查并回答：
   - 这个功能能读到其他用户的数据吗？
   - 能修改不属于自己的数据吗？
   - 删除数据前确认过所有权吗？

## 三、存档与交接

7. **存档点**：每完成一个能运行的功能，提醒我执行 `git add . && git commit` 存档。进行大范围改动前，必须先提醒我存档。
8. **交接文档**：当我要求结束或切换对话时，生成 `HANDOFF.md`，内容包括：
   - 已完成的功能（一句话一个）
   - 本次改动过的文件清单
   - 未完成的事项和下一步建议
   - 已知的坑（比如某个依赖有版本问题）

## 四、新手常见坑（经验总结，必须遵守）

11. **警惕编造的包名**：AI 有时会推荐根本不存在的包（拼写相近的假包可能是投毒包）。推荐任何包时必须给出：在 npmjs.com 或 pypi.org 上的真实存在性、作者、是否广泛使用。没把握就直说没把握。
12. **优先知名方案**：实现功能时优先选择使用人数最多的主流库和写法，不用冷门、停更、"我更喜欢"的方案。
13. **"完成"必须有证据**：说某个功能完成之前，必须实际运行过并贴出运行结果或截图证据。"应该可以了"不算完成。
14. **报错原文直传**：遇到报错时，要求我贴报错的完整原文（复制粘贴），不要我自己转述；你分析报错时也要引用原文，不许自己脑补报错内容。
15. **一次只做一个功能**：禁止在一个回合里塞进多个大功能。做完一个、验证一个、存档一个，再开始下一个。
16. **隐私数据不入库**：真实姓名、学号、手机号、身份证号、学校账号等信息不得写进代码和测试数据，一律用假数据。
17. **`.env` 不上传**：`.env` 文件必须写进 `.gitignore`，永远不提交到 git，更不能推到公开仓库。
18. **谨慎对待"升级"**：AI 建议升级依赖版本时，先问一句"升级会破坏什么"，不要为了新而新。
19. **卡住超过三轮就换路**：同一个报错修了三轮还没好，停下来总结已试过的方法，换思路或生成 HANDOFF.md 让我开新对话，不许继续死磕。

## 五、沟通方式

20. **面向零基础**：解释时不要堆术语，用到术语就顺带一句话解释。不要一次抛给我 5 个以上需要我做决定的问题。
21. **诚实汇报**：测试失败就说失败，贴出报错信息；没做的事不要说做了。

## 自定义区

- 本项目用途：（一句话描述）
- 技术栈：（比如：Python + Flask / 纯前端 HTML）
- 其他要求：
